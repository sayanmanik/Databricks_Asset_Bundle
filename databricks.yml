# This is a Databricks asset bundle definition for my_dab.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: my_dab
  uuid: 0e5db389-32b8-4c13-b867-0bbef1833721

artifacts:
  python_artifact:
    type: whl
    build: uv build --wheel

include:
  - resources/*.yml
  - resources/*/*.yml

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://dbc-940cb972-36ee.cloud.databricks.com

  prod:
    mode: production
    workspace:
      host: https://dbc-940cb972-36ee.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/sayanmanik.sm@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/sayanmanik.sm@gmail.com/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - user_name: sayanmanik.sm@gmail.com
        level: CAN_MANAGE
